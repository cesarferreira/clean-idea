require 'spec_helper'

describe Clean::Idea do
  it 'has a version number' do
    expect(Clean::Idea::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
